from django.contrib.auth import models


class DiscordBotManager(models)
    def create_new_DiscordBot(self):
            print('Creating new Discord Bot')
            new_db = self.create(
                id="DML",
                init=False
            )
            return new_db